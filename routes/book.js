const express = require('express');
const router = express.Router();
const handlerBook = require('../handlers/books')

//router.get('/allMovies', handlerBook.getMovies)

//router.get('/:idUser/books', handlerBook.getBooks)
//router.put('/:idUser/editbook/:idBook', handlerBook.putBook)
//router.post('/:idUser/addbook', handlerBook.postBook)
//router.delete('/:idUser/book/:idBook', handlerBook.deleteBook)

module.exports = router